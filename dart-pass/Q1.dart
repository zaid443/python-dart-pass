 main(){
   const hour=DateTime.now().hour;
   print(hour);
}
// Constant variables must be assigned to a value that never changes like:
// const pi = 22/7;
// In the above example time is changing so the value will never be constant